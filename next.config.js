/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  compress: true,
  // We'll add PWA configuration later with next-pwa
};

module.exports = nextConfig;
